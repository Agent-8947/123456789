# DNA Matrix Export (Flat Architecture)

### 🚀 Setup
1. `npm install`
2. `npm run dev`

### 📁 Structure (Based on 003ref-gen-main)
- Root: App.tsx, store.ts, index.tsx
- Folders: /components, /utils, /public

This structure is optimized for Vercel and zero-config module resolution.